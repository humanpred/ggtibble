% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_ggtibble.R
\name{as_ggtibble}
\alias{as_ggtibble}
\alias{as_ggtibble.gglist}
\title{Convert an object to a \code{ggtibble}}
\usage{
as_ggtibble(x, ...)

\method{as_ggtibble}{gglist}(x, ..., caption_prefix = NULL)
}
\arguments{
\item{x}{The object to convert (currently a \code{gglist}).}

\item{...}{Passed to methods.}

\item{caption_prefix}{Text prepended (separated by a space) to the name of
each element when building captions.  Defaults to \code{NULL} (no prefix), and
is set automatically to the outer caption when recursing into a nested
\code{gglist}.}
}
\value{
A \code{ggtibble} object with \code{figure} and \code{caption} columns.
}
\description{
Converts a named (and possibly nested) \code{\link[=gglist]{gglist()}} into a \code{\link[=ggtibble]{ggtibble()}} whose
\code{caption} column is built from the element names.  This is the bridge that
lets a collection of figures built up as a \code{gglist} flow into the reporting
helpers (\code{\link[=knit_print.ggtibble]{knit_print.ggtibble()}}, \code{\link[=ggsave.ggtibble]{ggsave.ggtibble()}}).
}
\details{
When the \code{gglist} is nested (an element is itself a \code{gglist}), the result is
flattened to a single \code{ggtibble} with one row per leaf figure and the outer
element name prepended to each inner name.  For example, a top-level element
named \code{"All Data"} whose value is a \code{gglist} containing a figure named
\code{"dv_pred_ipred_linear"} produces a caption of
\code{"All Data dv_pred_ipred_linear"}.
}
\section{Methods (by class)}{
\itemize{
\item \code{as_ggtibble(gglist)}: Convert a (possibly nested) \code{gglist} to a \code{ggtibble},
using element names as captions

}}
\examples{
g <-
  new_gglist(stats::setNames(
    list(
      ggplot2::ggplot(mtcars, ggplot2::aes(mpg, wt)) + ggplot2::geom_point(),
      ggplot2::ggplot(mtcars, ggplot2::aes(mpg, hp)) + ggplot2::geom_point()
    ),
    c("weight", "horsepower")
  ))
as_ggtibble(g)
}
